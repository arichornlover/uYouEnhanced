# Original idea by @ISnackable. Thanks to him for handling the hardest parts!
# https://github.com/ISnackable/YTCubePlus/blob/main/.github/workflows/Build.yml

name: Build and Release uYouEnhanced

on:
  workflow_dispatch:
    inputs:
      sdk_version:
        description: "iOS SDK Version"
        default: "18.6"
        required: true
        type: string
      uyou_version:
        description: "uYou Version"
        default: "3.0.4"
        required: true
        type: string
      decrypted_youtube_url:
        description: "Direct URL of the decrypted YouTube IPA"
        default: ""
        required: true
        type: string
      bundle_id:
        description: "Modify the bundle ID"
        default: "com.google.ios.youtube"
        required: true
        type: string
      app_name:
        description: "Modify the app name"
        default: "YouTube"
        required: true
        type: string
      commit_id:
        description: "(Optional) Commit ID to build at"
        default: ""
        required: false
        type: string
      upload_artifact:
        description: "Upload IPA as artifact"
        default: false
        required: false
        type: boolean
      create_release:
        description: "Create a draft release"
        default: true
        required: false
        type: boolean
      ytuhd:
        description: "Include YTUHD (this will clone dav1d + libvpx libraries)"
        default: false
        required: false
        type: boolean

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build uYouEnhanced
    runs-on: macos-latest
    permissions:
      contents: write

    steps:
      - name: Checkout main
        uses: actions/checkout@v7
        with:
          path: main
          ref: ${{ github.event.inputs.commit_id || github.ref }}
          submodules: recursive

      - name: Install system deps
        run: |
          brew update
          brew install ldid dpkg make wget unzip jq pipx

      - name: Set GNU make PATH and THEOS
        run: |
          echo "$(brew --prefix make)/libexec/gnubin" >> $GITHUB_PATH
          echo "THEOS=${{ github.workspace }}/theos" >> $GITHUB_ENV

      - name: Setup Theos
        uses: actions/checkout@v7
        with:
          repository: theos/theos
          ref: 9bc73406cf80b711ef4d02c15ff1dedc4478a275
          path: theos
          submodules: recursive

      - name: Cache SDK
        id: sdk-cache
        uses: actions/cache@v6
        with:
          path: theos/sdks/
          key: iOS-${{ inputs.sdk_version }}-SDK

      - name: Download iOS SDK
        if: steps.sdk-cache.outputs.cache-hit != 'true'
        env:
          THEOS: ${{ github.workspace }}/theos
        run: |
          git clone --quiet -n --depth=1 --filter=tree:0 https://github.com/aricloverALT/sdks/ sdks
          cd sdks
          git sparse-checkout set --no-cone iPhoneOS${{ inputs.sdk_version }}.sdk
          git checkout --quiet
          mv *.sdk "$THEOS/sdks"

      - name: Install Theos jailed
        uses: actions/checkout@v7
        with:
          repository: qnblackcat/theos-jailed
          ref: master
          path: theos-jailed
          submodules: recursive

      - name: Install Theos Jailed
        run: |
          ./theos-jailed/install
        env:
          THEOS: ${{ github.workspace }}/theos

      - name: Install cyan (pyzule-rw) and tbd
        run: |
          # pipx is installed by brew above; make sure pipx's bin dir is on PATH
          python3 -m pip install --upgrade pipx wheel setuptools
          python3 -m pipx ensurepath || true
          # install cyan tool (pyzule-rw)
          pipx install https://github.com/asdfzxcvbn/pyzule-rw/archive/main.zip || true
          # make pipx-installed binaries accessible
          echo "$HOME/.local/bin" >> $GITHUB_PATH
          # install tbd binary
          wget --quiet --no-verbose "https://github.com/inoahdev/tbd/releases/download/2.2/tbd-mac" -O /usr/local/bin/tbd || true
          chmod +x /usr/local/bin/tbd || true

      - name: Clone headers (YouTubeHeader & PSHeader)
        if: always()
        run: |
          set -euo pipefail
          THEOS=${{ github.workspace }}/theos
          # YouTube headers
          if [ ! -d "$THEOS/include/YouTubeHeader" ]; then
            git clone --quiet --depth=1 https://github.com/aricloverEXTRA/YouTubeHeader.git "$THEOS/include/YouTubeHeader"
          else
            cd "$THEOS/include/YouTubeHeader" && git pull --quiet --rebase || true
          fi
          # PSHeader
          if [ ! -d "$THEOS/include/PSHeader" ]; then
            git clone --quiet --depth=1 https://github.com/PoomSmart/PSHeader.git "$THEOS/include/PSHeader"
          else
            cd "$THEOS/include/PSHeader" && git pull --quiet --rebase || true
          fi

      - name: Hash YT ipa url
        run: |
          URL_YT="$(jq -r '.inputs.decrypted_youtube_url' $GITHUB_EVENT_PATH)"
          echo ::add-mask::$URL_YT
          echo URL_YT=$URL_YT >> $GITHUB_ENV

      - name: Prepare YouTube iPA
        id: prepare_youtube
        env:
          THEOS: ${{ github.workspace }}/theos
          YOUTUBE_URL: ${{ env.URL_YT }}
        run: |
          set -euo pipefail
          mkdir -p main
          wget "$YOUTUBE_URL" --quiet -O main/YouTube.ipa
          cd main
          mv YouTube.ipa YouTube.zip
          unzip -q YouTube.zip
          youtube_version=$(defaults read "$(pwd)/Payload/YouTube.app/Info" CFBundleVersion)
          echo "youtube_version=${youtube_version}" >> $GITHUB_OUTPUT
          echo "==> YouTube v$youtube_version downloaded!"

      - name: Fix compiling & Build Package
        id: build_package
        env:
          THEOS: ${{ github.workspace }}/theos
          UYOU_VERSION: ${{ inputs.uyou_version }}
          BUNDLE_ID: ${{ inputs.bundle_id }}
          APP_NAME: ${{ inputs.app_name }}
        run: |
          set -euo pipefail
          cd ${{ github.workspace }}/main
          # Update Makefile reliably (macOS sed)
          if grep -q '^BUNDLE_ID' Makefile; then
            sed -i '' "s/^BUNDLE_ID *=.*$/BUNDLE_ID = $BUNDLE_ID/" Makefile || true
          else
            echo "BUNDLE_ID = $BUNDLE_ID" >> Makefile
          fi
          if grep -q '^DISPLAY_NAME' Makefile; then
            sed -i '' "s/^DISPLAY_NAME *=.*$/DISPLAY_NAME = $APP_NAME/" Makefile || true
          else
            echo "DISPLAY_NAME = $APP_NAME" >> Makefile
          fi
          if grep -q '^export TARGET' Makefile; then
            sed -i '' "s/^export TARGET *=.*$/export TARGET = iphone:clang:${{ inputs.sdk_version }}:16.0/" Makefile || true
          else
            echo "export TARGET = iphone:clang:${{ inputs.sdk_version }}:16.0" >> Makefile
          fi
          # Ensure YTUHD flag presence; use Makefile variable so subprojects pick it up
          if [ "${{ inputs.ytuhd }}" = "true" ]; then
            echo "YTUHD_ENABLED=1" >> Makefile
          else
            echo "YTUHD_ENABLED=0" >> Makefile
          fi
          # Run package (do not swallow errors so CI fails on true compile problems)
          make package THEOS_PACKAGE_SCHEME=rootless IPA=Payload/YouTube.app FINALPACKAGE=1
          echo "package=$(ls -t packages | head -n1)" >> $GITHUB_OUTPUT
          echo "==> SHASUM256: $(shasum -a 256 packages/*.ipa | cut -f1 -d' ')"
          echo "==> Bundle ID: $BUNDLE_ID"

      - name: Clone Open in YouTube
        run: |
          set -euo pipefail
          git clone --quiet -n --depth=1 --filter=tree:0 https://github.com/BillyCurtis/OpenYouTubeSafariExtension.git OpenYouTubeSafariExtension || true
          cd OpenYouTubeSafariExtension || exit 0
          git sparse-checkout set --no-cone OpenYouTubeSafariExtension.appex || true
          git checkout --quiet || true
          mv *.appex "${{ github.workspace }}/" 2>/dev/null || true

      - name: Prepare YTUHD vendor libs and build (optional)
        if: ${{ inputs.ytuhd }}
        env:
          THEOS: ${{ github.workspace }}/theos
        run: |
          set -euo pipefail
          echo "⚠️ Installing YTUHD + required libraries (dav1d + libvpx)..."
          mkdir -p "$THEOS/vendor"
          git clone --quiet --depth=1 https://code.videolan.org/videolan/dav1d.git "$THEOS/vendor/dav1d" || true
          git clone --quiet --depth=1 https://chromium.googlesource.com/webm/libvpx "$THEOS/vendor/libvpx" || true
          # Ensure headers are present (we cloned them above), then clone YTUHD into workspace and build
          rm -rf "${{ github.workspace }}/YTUHD" || true
          git clone --quiet --depth=1 https://github.com/PoomSmart/YTUHD.git "${{ github.workspace }}/YTUHD" || true
          cd "${{ github.workspace }}/YTUHD"
          make clean package DEBUG=0 FINALPACKAGE=1 THEOS_PACKAGE_SCHEME=rootless
          # move deb(s) to workspace for injection convenience
          find packages -type f -name '*.deb' -exec cp -n {} "${{ github.workspace }}/" \; || true
          echo "YTUHD + libraries prepared."

      - name: Collect built tweak packages
        run: |
          set -euo pipefail
          # Copy any .deb from known package dirs into root for cyan injection
          for src in main/packages packages YTUHD/packages main; do
            if [ -d "$src" ]; then
              find "$src" -type f -name '*.deb' -print0 2>/dev/null | xargs -0 -I{} cp -n {} . || true
            fi
          done
          echo "DEBs present for injection:"
          ls -1 *.deb 2>/dev/null || echo "(none)"

      - name: Inject tweaks into IPA (cyan) and produce final IPA
        run: |
          set -euo pipefail
          # Find produced IPA (prefer the package created by make)
          IPA_IN="main/packages/${{ steps.build_package.outputs.package }}"
          if [ ! -f "$IPA_IN" ]; then
            IPA_IN=$(ls -t main/packages/*.ipa 2>/dev/null | head -n1 || echo "")
          fi
          if [ -z "$IPA_IN" ]; then
            echo "::error::No IPA found to inject"
            exit 1
          fi
          OUTNAME="uYouEnhanced_${{ steps.prepare_youtube.outputs.youtube_version }}_v${{ inputs.uyou_version }}.ipa"
          tweaks="OpenYouTubeSafariExtension.appex"
          for f in *.deb; do
            [ -f "$f" ] && tweaks="$tweaks $f"
          done
          echo "Injecting tweaks: $tweaks into $IPA_IN -> $OUTNAME"
          cyan -i "$IPA_IN" -o "$OUTNAME" -uwef $tweaks -n "${{ inputs.app_name }}" -b ${{ inputs.bundle_id }}
          ls -la "$OUTNAME" || true

      - name: Upload artifact
        if: ${{ inputs.upload_artifact }}
        uses: actions/upload-artifact@v7
        with:
          name: uYouEnhanced_${{ steps.prepare_youtube.outputs.youtube_version }}_v${{ inputs.uyou_version }}
          path: uYouEnhanced_*.ipa

      - name: Create Draft Release
        if: ${{ inputs.create_release }}
        uses: softprops/action-gh-release@v3
        with:
          tag_name: v${{ steps.prepare_youtube.outputs.youtube_version }}-uYou${{ inputs.uyou_version }}-${{ github.run_number }}
          name: v${{ steps.prepare_youtube.outputs.youtube_version }}-uYou${{ inputs.uyou_version }}-${{ github.run_number }}
          files: uYouEnhanced_*.ipa
          draft: true
